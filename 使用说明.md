# 中文使用指南（公开源码版）

安装与启动请先阅读[README](README.md)。本仓库不附Python运行时、历史开奖全集、权重或个人实验记录；首次启动与个人便携包的初始内容不同。

## 1. 无数据体验

打开“组合方案与中奖概率”，选择彩种和对应玩法，保留“均匀随机去重”，填写注数后生成方案。最高奖概率按合法且不同的整票覆盖计算，成本按页面说明估计。改变种子只改变号码，不创造更高单注中奖概率。

排列3直选、组选3、组选6覆盖的排列数和奖金不同；不能把组选更高的命中概率称为模型预测更准确。

## 2. 获取或载入历史

在“分步操作”点击获取数据，联网请求第三方历史网站；请遵守来源条款，不要频繁重试或绕过访问限制。失败时不会把空数据当成功。

也可准备自己有权使用的UTF-8 CSV，放在项目根目录的`data/`中，文件名和必需列如下：

| 文件 | 必需列 |
|---|---|
| `ssq_history.csv` | `issue,date,red1,red2,red3,red4,red5,red6,blue` |
| `dlt_history.csv` | `issue,date,front1,front2,front3,front4,front5,back1,back2` |
| `pls_history.csv` | `issue,date,digit1,digit2,digit3` |
| `plw_history.csv` | `issue,date,digit1,digit2,digit3,digit4,digit5` |
| `qxc_history.csv` | `issue,date,digit1,digit2,digit3,digit4,digit5,digit6,digit7` |

`issue`保留字符串格式，`date`使用`YYYY-MM-DD`。球彩每区升序、行内不重复；数字彩保留位置和0。7星彩适用前六位0–9、末位0–14的规则，不能混入旧规则历史。不要把合成测试数据冒充实际开奖。

点击“读取本地数据（不联网）”完成校验。缺少文件、非法范围、日期错误或冲突期号会报错，不要忽略这些错误直接训练。

## 3. 训练和选号

默认配置为窗口30、最多30轮、学习率0.001、隐藏层64、Batch128、单模型、无Attention。它们是受控研究起点，不是已证明能中奖的最佳参数。训练需要足够多的有效历史，具体缺口以界面提示为准。

训练完成后使用对应模型模式生成号码；预测输出是模型倾向排序，不是校准后的真实中奖概率。历史回测仅为探索，反复调参会消耗留出集的独立性。

## 4. 恢复与备份

训练成功后保存`models/<彩种>_training_session.json`及权重。重启后点击“恢复已保存模型（不重训）”；程序校验数据、特征、模型文件和参数是否匹配。数据修订或新增后不能假装模型还是同一训练会话，应重新训练。

备份自己的`data/`、`models/`、`results/`及研究目录到私人位置，不要提交到公开GitHub。多个独立程序同时操作一个目录尚无跨进程锁，请只运行一个副本。

## 5. 研究页

“预测优势验证”管理本地冻结协议与未来记录；“国际算法对照”只做固定历史比较，不自动登记新候选的未来预测。首次公开源码安装不会带上维护者的旧研究，先阅读[研究步骤](docs/research/README.md)。

## 常见问题

- 缺少模块：确认安装与启动都使用同一个`.venv`中的Python；运行`python -m pip check`。
- 无历史数据：这是公开源码版的预期初始状态；随机去重模式可用，训练需自行准备数据。
- 查看不到旧实验：仓库仅公开方法和汇总结论，不包含个人研究工件；不要创建假记录绕过校验。
- 第三方页面失效：保留错误信息、使用合法本地数据；不要关闭证书验证或绕过访问限制。
- 中文图表字体缺失：使用安装有中文字体的Windows环境；这不影响概率计算，渲染情况需本机检查。
- 便携构建：`packaging/`沿用本地私用构建流程，会收集运行数据。它不是安全的公开发布工具，也不保证陌生电脑开箱即用。
