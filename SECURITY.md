# 安全与隐私

当前维护0.10.x源码。这里只承诺处理报告，不承诺响应时限或漏洞修复SLA。

- 仅绑定本机`127.0.0.1`；不要开启公共分享、端口转发或直接部署到公网。
- 仅加载自己生成或可信来源的模型、缩放器、训练档案；不要反序列化陌生人提供的权重或pickle/joblib文件。
- 本地JSON、文件哈希和哈希链能发现部分意外改动，但不能防止整套重写、删除，也不提供可信开奖前时间戳。
- 不在Issue附上凭证、完整日志、个人选号记录或私人文件。截图请先遮盖个人信息。
- 如发现可利用漏洞，优先使用仓库Security页的私密漏洞报告入口（若可用）；不要公开利用细节。若入口不可用，只公开说明需要安全联系，不贴敏感内容。
- 不因程序错误关闭杀毒软件，不下载未知DLL，不授予管理员权限绕过问题。

依赖漏洞和许可证状态会变化。安装与再分发前应检查实际使用版本；测试通过不代表完成全面安全审计。
